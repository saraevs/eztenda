<div class="container">
  <div class="row">
    <h1 class="mt-5">Welcome back, <%= @user.name %></h1>
  </div>

  <% if !current_user.is_venue %>
    <%= render "user_is_brand" %>
  <% else %>
    <%= render "user_is_venue" %>
  <% end %>
</div>
